import { ConfigData } from './types';

export default function readAsyncData({ processes, ...config }: ConfigData, options = {}) {
  let data: any = null;
  let errors: any[] = [];
  return new Promise(() => {});
}
